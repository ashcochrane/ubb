"""Repository tooling that is not part of any deployable package.

Importable from the git root with no Django, no database and no application
settings, so the platform, the SDK and the console tooling can all reach it.
"""
